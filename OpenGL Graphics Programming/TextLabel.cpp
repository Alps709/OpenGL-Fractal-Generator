#include "TextLabel.h"


TextLabel::TextLabel(std::string _newText, std::string _newFont, glm::vec2 _newPosition, glm::vec3 _newColour, float _newScale)
{  
	m_text = _newText;
	m_position = _newPosition;
	m_colour = _newColour;
	m_scale = _newScale;

	//Edit to work with current code
}
